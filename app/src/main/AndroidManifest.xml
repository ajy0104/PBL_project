<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.pbl_project">

    <uses-permission android:name="android.permission.INTERNET" />

    <!--와이파이의 상태에 접근할 수 있는 퍼미션 부여 -->
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE"/>
    <!-- 지문 인식 권한 -->
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".vacation_list"/>

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyBjMOR3yCYIrB4xodZzpObrsptCRzqCGtE" />

        <activity android:name=".vacation" />
        <activity android:name=".MainActivity" />
        <activity android:name=".LoginActivity">

        </activity>
        <activity android:name=".RegisterActivity" />
        <activity android:name=".Add_schedule" />
        <activity android:name=".MapActivity" />
        <activity android:name=".AttendanceActivity" />
        <activity android:name=".ScheduleActivity"/>
        <activity android:name=".Bio_authActivity">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />

            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
        </activity>
    </application>

</manifest>